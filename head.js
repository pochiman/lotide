const assertEqual = require('./assertEqual');

// FUNCTION IMPLEMENTATION: takes in an array and returns the first element in the array
const head = function(array) {
  return array[0];
};

module.exports = head;